apiVersion: v1
kind: ConfigMap
metadata:
  name: kk-payments-config
  namespace: kijani-staging
  labels:
    environment: staging
    managed-by: ansible
data:
  NODE_ENV: staging
  DB_HOST: postgres-staging.kijani.internal
  DB_PORT: "5432"
  LOG_LEVEL: debug
  APP_PORT: "3000"
  MAX_CONNECTIONS: "5"
